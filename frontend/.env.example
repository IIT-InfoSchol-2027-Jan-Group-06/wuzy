# Copy to .env (gitignored) and set the backend the app should talk to.
# Leave unset to use the dev machine that serves the bundle on port 8000.
EXPO_PUBLIC_API_URL=https://wuzy-production.up.railway.app/
